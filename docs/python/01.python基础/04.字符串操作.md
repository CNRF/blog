---
title: '04.字符串操作'
tags: [ '' ]
categories: [ '' ]
---

## 格式化

字符串格式化是将变量值插入到字符串中的一种方法。Python 提供了多种字符串格式化的方法：

### f-字符串（Python 3.6 及以上版本）

```python
name = "Alice"
age = 30
formatted_string = f"My name is {name} and I am {age} years old."
print(formatted_string)  # 输出: My name is Alice and I am 30 years old.
```

### str.format() 方法

```python
name = "Bob"
age = 25
formatted_string = "My name is {} and I am {} years old.".format(name, age)
print(formatted_string)  # 输出: My name is Bob and I am 25 years old.
```

### 百分号 (%) 格式化

```python
name = "Charlie"
age = 28
formatted_string = "My name is %s and I am %d years old." % (name, age)
print(formatted_string)  # 输出: My name is Charlie and I am 28 years old.
```

## 索引和切片

字符串可以通过索引访问单个字符，也可以通过切片访问子字符串。

### 索引

```python
text = "Hello, World!"
first_char = text[0]  # 'H'
last_char = text[-1]  # '!'
print(first_char, last_char)  # 输出: H !
```

### 切片

切片允许你提取字符串的子部分。

1. 切片的起始索引是包含的，结束索引是不包含的。
2. 如果省略起始索引，默认从字符串开头开始；如果省略结束索引，默认切片到字符串末尾。
3. 切片可以使用负数索引，从字符串末尾开始计数。

```python
text = "Hello, World!"
substring = text[0:5]  # 'Hello'
print(substring)  # 输出: Hello
substring2 = text[7:]  # 'World!'
print(substring2)  # 输出: World!
substring3 = text[-6:-1]  # 'World'
print(substring3)  # 输出: World
substring4 = text[::-1] # '!dlroW ,olleH'
print(substring4)  # 输出: !dlroW ,olleH
substring5 = text[::1] 
print(substring5)  # 输出: Hello, World!
substring6 = text[::2]
print(substring6)  # 输出: Hlo ol!
```

## 查找

find()：返回子字符串在字符串中首次出现的索引位置，如果未找到则返回 -1。

```python
text = "Hello, World!"
index = text.find("World")  # 7
print(index)  # 输出: 7
```

index()：类似于 find()，但如果未找到子字符串会引发 ValueError 异常。

```python
text = "Hello, World!"
index = text.index("World")  # 7
print(index)  # 输出: 7
```

## 替换

replace()：将字符串中的指定子字符串替换为另一个子字符串。

```python
text = "Hello, World!"
new_text = text.replace("World", "Python")  # 'Hello, Python!'
print(new_text)  # 输出: Hello, Python!
```
